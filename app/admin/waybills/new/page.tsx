import { CreateWaybillPage } from '@/components/pages/CreateWaybillPage'

// Создание накладной. Номер резервирует сервер при открытии формы (ПРАВКИ 2, п.7).
export default function Page() {
	return <CreateWaybillPage />
}
