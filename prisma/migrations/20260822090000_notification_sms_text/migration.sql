-- Короткий SMS-вариант уведомления рядом с отправленным WhatsApp-текстом.
-- Нужен для досылки по вебхуку (ack = -1): без него SMS уходила бы пересобранным
-- шаблоном, теряя правки оператора, сделанные перед отправкой.
ALTER TABLE "Notification" ADD COLUMN IF NOT EXISTS "smsText" TEXT;
